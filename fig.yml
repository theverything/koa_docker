web:
  build: .
  command: "nodemon --hamrmony ./server.js"
  volumes:
    - .:/app"
  links:
    - "redis"
  ports:
   - "8080:8080"
redis:
  image: "dockerfile/redis"
  volumes:
    -   redis/data:/data
